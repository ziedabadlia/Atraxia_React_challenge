import { SxProps } from "@mui/system";

export const TopNavStyle: SxProps = {
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
    padding: "25px",
    borderBottom: "1px solid #EDEDED",
  };